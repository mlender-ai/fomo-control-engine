# WO-FCE-XX — <작업명>

우선순위: P0/P1/P2 (한 줄 근거)
선행: <선행 WO 또는 "없음">

## 착수 전 확인 (AGENTS.md 불변 규칙 2)
- [ ] `git branch -a` 에 같은 WO 브랜치 없음
- [ ] `git log --oneline -15` + `git status` 에 기존 구현 없음
- [ ] `grep -r "WO-FCE-XX" docs/ backend/ dashboard/` 결과 확인

## 진단 (코드 확정)
- 현재 상태와 문제를 파일:라인 근거로. 추측이면 "가설"로 표기.

## 작업
### 1. <단위 작업>
- 무엇을, 어디에(파일), 어떻게.

## 수용 기준
- [ ] <검증 가능한 조건 — "테스트 X 통과", "화면 Y에 Z 노출">

## 금지
- <이 작업에서 절대 하지 않을 것 — 도메인 불변(자동 승격·룩어헤드·실주문) 포함 여부 확인>

## 문서
- 갱신할 `docs/*.md`: <목록>

## 완료 정의 (공통)
- [ ] HARNESS.md 게이트 통과
- [ ] docs 갱신
- [ ] origin/main 반영 + CI success 확인 (불변 규칙 1·3)
