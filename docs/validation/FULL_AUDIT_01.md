# 4주 실사 (WO-FCE-FULL-AUDIT-01)

> **계측 전용. 아무것도 고치지 않았다.**
> 산출물은 숫자 하나가 아니라 **트랙별 판정 가능/불가와 그 사유**다.
> 데이터가 없는 칸은 "미산출 + 사유"로 남겼다 — 빈칸이 지어낸 숫자보다 낫다.

실사 시각: 2026-08-15 15:01 UTC · 실행 커밋 `d2ebcebb` == `origin/main` · DB 10.0GB

---

## 결론 — 판정 가능한 트랙: **0개**

| 트랙 | 유효일 | 진입 | 청산 | 독립 표본 | 국면 | 판정 |
| --- | --- | --- | --- | --- | --- | --- |
| 크립토 | 21/28 | 36 | 34 | **18~25** (정의별) | 1종 (`downtrend`) | `SLOW` |
| 주식 KR | 8/28 | 5 | **0** | **0** | 라벨 미기록 | `INSUFFICIENT_DATA` |
| 주식 US | 2/28 | 6 | 3 | 3 | 라벨 미기록 | `INSUFFICIENT_DATA` |
| 폴리 | 9/28 | 9 | 1 | 1 | 라벨 미기록 | `STRUCTURALLY_BLOCKED` |

**달력 4주가 지났으나 목표(유효일 28 · 표본 30 · 국면 2)를 채운 트랙이 없다.**
가장 앞선 크립토조차 표본이 어느 독립성 정의로도 30에 못 미친다.

---

## Phase 1 — 원장 무결성

### 1-1. 왕복 오염 — 이 실사의 최중요 숫자

정본: [`SAMPLE_INDEPENDENCE.md`](SAMPLE_INDEPENDENCE.md)

크립토 청산 34건 중 **같은 확정봉 왕복 10건**(전부 동일 방향, 9건 동일가).
독립 표본은 정의에 따라 **18 / 20 / 25건**이며 **모든 정의에서 30 미만**이다.

누적 수익률은 어느 정의로도 **음수**(−28% ~ −50%)다. 알림의 "승률 53%"는 승률만 본 값이다.

> **⚠️ 위 수치는 구 정의(퍼센트 단순합) 기반이다.** `WO-FCE-METRIC-TRUTH-01` 이 지표를
> 금액 기준으로 바꿨다. 크립토는 고정 사이즈라 금액과 퍼센트합이 1:1 대응하므로 **부호와
> 순위는 유지**되지만, **자본 대비로는 −9.37%** 이고 낙폭도 81.61 USDT(자본 대비 16.3%)다.
> 정본: [`METRIC_DEFINITIONS.md`](METRIC_DEFINITIONS.md)

### 1-2. 매달림이 관측일에 미친 영향

| 항목 | 값 |
| --- | --- |
| `heartbeat_stale` 재시작 | **156건** (WORKER-HANG-02 실측 시점 101건 → 55건 증가) |
| 발생 구간 | 2026-07-28 ~ 08-15 |
| **08-11 이후 폭증** | 08-11 **18** · 08-12 **43** · 08-13 **31** · 08-14 **41** · 08-15 **14** |
| 잡 전면 정지 구간 | **147건 · 합계 30.4시간** (`job-trace.jsonl` 46시간 창의 66%) |
| 최장 정지 | 2,952초 (49분) |

08-11 이후 크립토 커버리지가 붕괴했다: 71% → 7% → 52% → 16% → 47% — 전부 유실일.

**커버리지 게이트는 제 역할을 했다.** 매달림이 있던 14일 중 크립토가 "유효"로 계수된 날은
5일뿐이고, 그 날들의 매달림은 5건(전체의 3%)이다. 즉 **대부분의 매달림은 이미 유실일로
정확히 걸러졌다.** 유효일 수치는 매달림 때문에 부풀려지지 않았다.

### 1-3. 창 앵커 — 미적용

`validation_windows` 테이블 **행 수 0**. 4트랙 모두 `앵커 없음`.
`WO-FCE-WINDOW-ANCHOR-01`(PR #13)이 머지됐으나 **실제 앵커가 하나도 기록되지 않았다.**
따라서 현재 모든 검증 시계는 앵커 없이 첫 유효일 기준으로 계산되고 있다.

> 머지 ≠ 적용. 이 실사에서 세 번째로 확인된 같은 유형의 간극이다.

### 1-4. DB 무변경 — 정직한 진술

**이 실사는 DB 에 쓰지 않았다.** 모든 조회를 `mode=ro` 로 열었고 쓰기 구문을 실행하지 않았다.

다만 **행 수 해시는 달라졌다**(`cfe65c6f…` → `f9c15392…`). 엔진이 가동 중이었으므로
수집 테이블이 계속 증가했기 때문이다. 따라서 **해시 일치는 이 실사의 무쓰기를 증명하지
못한다** — 증명은 "읽기 전용 연결 + 쓰기 구문 부재"이지 해시가 아니다. 그렇게 적는 것이 정직하다.

10분간 증가분(엔진 자체 쓰기):

| 테이블 | 증가 |
| --- | --- |
| `stock_paper_events` | **+103,512** |
| `bitget_trade_fills` | +779 |
| `stock_paper_orders` | **+48** |
| 나머지 7개 | 각 +1~16 |

> **`stock_paper_events` 가 10분에 10만 건 증가한다.** 이것이 DB 10GB 의 실제 원인이며,
> §2-2 의 장외 매도 무한 중복(20초당 1건 주문 + 그에 딸린 이벤트)과 같은 뿌리로 보인다.
> **동시 관측일 뿐 인과를 단정하지 않는다**(C8). 별도 WO 로 넘긴다.

---

## Phase 2 — 트랙별 실사

### 2-1. 크립토

- 진입 36 · 청산 34 · 보유 2
- 방향 편중: **long 26건(76%)** / short 8건(24%)
- 청산 사유: `time_decay` 10 · `take_profit_pressure` 9 · `invalidation_breach` 8 ·
  `duplicate_bootstrap_suppressed` 2 · `time_stop` 2 · `breakeven_stop` 2 · `take_profit_2` 1
- **국면 1종(`downtrend`)만 관측** — 완료 정의의 국면 2종을 못 채운다. 상승장 표본이 없다.

### 2-2. 주식 KR — 청산 도달 **0건**

| 항목 | 값 |
| --- | --- |
| 매수 체결 | 5건 |
| 매도 체결 | **0건** |
| 매도 주문 `queued` | **4,314건** |
| queued 사유 | **전부 `session_closed`** |
| 고유 심볼 | **2개** |
| 생성일 | 전량 2026-08-15 하루 |

**표본이 영원히 0인 구조다.** 청산이 체결되지 않으므로 승률·R이 원리적으로 산출 불가다.

원인 (파일:라인):

```
app/stock_paper/service.py:374   for row in store.open_positions():   ← 매 주기 열린 포지션마다 매도 주문 생성
app/stock_paper/execution.py:42  return ExecutionResult(..., status=OrderStatus.QUEUED, reason="session_closed")
```

장외라 `QUEUED` 로 대기하는데 포지션은 열린 채 남고, **기존 대기 주문이 있는지 확인하는 로직이
없어서** 다음 주기에 또 만든다. 포지션 2개에 하루 4,314건 = 약 20초당 1건.

`stock_paper_orders` 에 `pending_orders`·`has_open_order` 류의 중복 방지 조회가 **0건**이다.

> **이 WO 에서 고치지 않는다**(C3·C9). 발견 사실만 기록하고 별도 WO 로 넘긴다.

### 2-3. 주식 US

- 매수 체결 6 · **매도 체결 3** — 청산 경로는 US 에서 동작한다
- 유효일 2/28 (KST 롤오버 수리가 08-05 였으므로 그 이후만 유효)
- 표본 3건 — 판정 불가

KR 은 막히고 US 는 도는 차이가 위 §2-2 의 결함을 뒷받침한다.

### 2-4. 폴리 — 재시작해도 해소되지 않는다

| 항목 | 값 |
| --- | --- |
| 보유 | 8건 |
| 정산 완료 | 1건 |
| 보유분 만기 | **전량 2027-01-01** |
| **검증 창 내 정산 가능 건수** | **0건** |
| `poly_resolutions` 적재 | 12,038건 (관측 원장은 정상) |

정산 로직은 정상이다(1건 정산 확인, 원장 12,038건). 문제는 **선정된 시장의 만기가 검증 창 밖**이라는 것.
**검증 창을 다시 시작해도 동일하다** — 만기 2027-01-01 은 어떤 4주 창에도 들어오지 않는다.
유니버스 선정 기준을 바꾸지 않는 한 폴리는 구조적으로 표본을 만들 수 없다(사용자 결정 5).

### 2-5. 고래

- 지갑 110개 · 이벤트 `increase` 9,863 · `reduce` 8,016 · `close` 372 · `open` 303 · `flip` 81
- 선정 기준은 `quality_score` 유지, 승률은 사후 채점 — `OBSERVATION-INTEGRITY-01` Phase 5 경계 유지됨
- 푸시 강등(PR #11) 이후 `whale_entry` 는 `blocked_alerts` 로만 적재 — 회귀 없음

---

## Phase 3 — 복기 루프 실사

| 항목 | 값 |
| --- | --- |
| `calibration_suggestions` | **8건** (adopted 3 · dwell_blocked 5) |
| `engine_params` 버전 | 5 |
| `autonomy_log` | 10건 |
| `judgment_ledger` | **138,092건** |
| `judgment_scores` | 89,408건 |

**루프는 돌았다.** 판단 원장 13.8만 건, 채점 8.9만 건이 쌓였고 제안 8건 중 3건이 채택됐다.
5건은 `dwell_blocked` — 자기 완화 방향이라 dwell 창에 걸려 보류된 것으로, 설계대로 작동했다.

### 구조 결함 5종의 포착 건수: **0건**

이번 세션에서 확정된 결함(와이코프 선행 추세 미수용 · 커버리지 게이트 부재 · 재진입 잠금 부재 ·
`distance_pct` 스냅샷 오염 · 이벤트 루프 블로킹) 중 **캘리브레이션 제안으로 포착된 것은 없다.**

이것이 §2 사실 1의 실증이다 — **다이얼을 돌리는 루프는 다이얼이 없다는 것을 알아챌 수 없다.**
자율 루프가 고장난 게 아니라, 이 결함들이 그 시야 밖에 있다. 파라미터 루프에 구조 결함 탐지를
기대하면 안 된다.

---

## Phase 4 — 4주 결산

### 남은 것

| 항목 | 값 |
| --- | --- |
| 독립 표본 총계 | 크립토 18~25 · US 3 · 폴리 1 · KR 0 |
| 유효 관측일 | 크립토 21 · KR 8 · US 2 · 폴리 9 |
| **판정 가능 트랙 수** | **0** |
| 판단 원장 | 138,092건 (재판정 자산으로 보존 가치 있음) |

### 재시작 시 버리는 것 / 가져가는 것

**버리는 것**

- 크립토 유효일 21일 — 창이 리셋되면 0부터
- 크립토 청산 표본 34건(독립 18~25) — 재진입 잠금이 도입되면 **정책이 달라져 비교 불가**
- 주식 KR 유효일 8일 · US 2일 · 폴리 9일

**가져가는 것**

- `judgment_ledger` 138,092건 + `judgment_scores` 89,408건 — 재판정 하네스
  (`DIRECTIONAL-INTEGRITY` Phase 0)로 소급 재평가 가능
- `observation_coverage` 소급 산출 능력 — 창이 바뀌어도 같은 게이트로 다시 잰다
- 고래 관측 원장 · 폴리 정산 원장 12,038건

**핵심**: 재시작으로 잃는 것은 **표본 34건과 유효일**이고, 이미 그 표본은 왕복 오염으로
독립성이 의심되며 국면도 1종뿐이다. **잃을 것이 생각보다 적다는 것이 이 실사의 결론이다.**

---

## 미결 결정 (사용자 판단 필요)

| # | 결정 | 이 실사가 준 자료 |
| --- | --- | --- |
| 1 | 목표 N · 통계 방법 | 독립 표본 실제 18~25 (정의별) |
| 2 | 호스트 지속성 | 매달림 156건, 08-11 이후 폭증 — 절전과 별개 |
| 3 | 커버리지 임계 3종 | (`DIRECTIONAL-INTEGRITY` Phase 0 분포 필요 — 미산출) |
| 4 | 와이코프의 지위 | (미산출 — 기여도 계측이 이 WO 범위 밖) |
| 5 | 폴리 처리 | **검증 창 내 정산 가능 0건. 재시작해도 동일** |
| 6 | 검증 창 처리 | 위 "버리는 것/가져가는 것" |
| **7** | **재진입 잠금 도입** | **같은 봉 왕복 10건 / 34건 = 29%** |
| **8** | **표본 독립성 정의** | 정의별 N = 18 / 20 / 25 / 34 |

---

## 실사 중 발견됐으나 고치지 않은 결함 (C3·C9)

| # | 결함 | 위치 | 영향 |
| --- | --- | --- | --- |
| 1 | 장외 매도 주문 무한 중복 생성 | `stock_paper/service.py:374` + `execution.py:42` | KR 매도 4,314건 적체, 청산 0건 |
| 2 | 창 앵커 미적용 | `validation_windows` 행 0 | PR #13 이 실제로 동작하지 않음 |
| 3 | 재진입 잠금 부재 | `paper/service.py:182` | 같은 봉 왕복 10건 |
| 4 | 매달림 08-11 이후 폭증 | `WORKER-HANG-02` 소관 | 정지 30.4시간/46시간 |
| 5 | 크립토 국면 편중 | — | `downtrend` 1종만, long 76% |
| 6 | `stock_paper_events` 폭증 | (1번과 같은 뿌리로 추정, 단정 아님) | 10분에 +103,512행. DB 10GB |

**전부 별도 WO 로 넘긴다.** 이 실사에서 하나도 고치지 않았다.

---

## 완료 확인

- DB 쓰기 **0건** — 전 조회를 `mode=ro` 로 열었고 쓰기 구문 없음.
  (행 수 해시는 엔진 가동으로 달라졌다. §1-4 참조 — 해시로는 증명되지 않는다)
- 판정·진입 로직 diff **0줄** — `backend/app/analyst/` · `backend/app/structure/` · `backend/app/paper/policy.py`
- 봉인 무변경 — `GATE_APPROVED=False` 유지
- 알림 신설 **0건**
