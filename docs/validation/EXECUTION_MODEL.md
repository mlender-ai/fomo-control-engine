# 체결 모델 (Execution Model)

> WO-FCE-RISK-SIZING-01 Phase 3-5. **계측 + 결정 등록. 비용률 변경 0건**(C8).
>
> 실사 2026-08-18 · 앵커 창 · crypto · Phase 3-2 잠금 적용 후 **N=24**

---

## 1. 현행 체결 전제 (3-5 작업 1)

```python
paper/service.py   진입 체결가 = 확정봉 종가 (bar.close)
policy.py:35-36    taker_fee_pct = 0.06 · slippage_pct = 0.03      # 편도 0.09%
policy.py          execution_cost_rate = (taker + slippage) / 100
```

**확정봉 종가 체결은 시장가 주문 = 테이커를 전제한다.** 봉이 닫히는 순간의 가격에
무조건 들어간다는 것은 지정가로는 보장되지 않는다.

청산도 같다 — 익절은 봉 중간 터치 시 그 가격, 손절은 종가(`STOP_EXECUTION.md` D0 참조).

---

## 2. 거래소 실제 수수료율 (3-5 작업 2)

Bitget USDT-FUTURES 계약 명세 조회 (2026-08-18 · 공개 엔드포인트 · 759개 계약):

| 심볼 | takerFeeRate | makerFeeRate | sizeMultiplier |
| --- | --- | --- | --- |
| BTCUSDT | **0.0006** | 0.0002 | 0.0001 |
| ETHUSDT | **0.0006** | 0.0002 | 0.01 |
| HYPEUSDT | **0.0006** | 0.0002 | 0.01 |
| BASEDUSDT | **0.0006** | 0.0002 | 1 |
| SPCXUSDT | **0.0006** | 0.0002 | 0.01 |

**테이커 0.06% — 설정값 `taker_fee_pct = 0.06` 과 정확히 일치한다.**
수수료 전제는 옳다. 등급 할인은 반영하지 않았다(보수적 방향).

메이커는 **0.02%** 로 테이커의 1/3 이다.

> `sizeMultiplier` 는 최소 주문 증분이다. Phase 1 의 `min_notional_usdt` 처리와 관련되며
> 현재 반올림을 모델링하지 않는다 — **미반영 항목**으로 남긴다.

### 검증되지 않은 것 — 슬리피지 0.03%

수수료는 확인됐지만 **슬리피지 0.03% 는 근거가 없다.** 어디서 온 값인지 문서에 없고
실제 체결 기록과 대조된 적도 없다. 아래 민감도표가 보이듯 **비용의 1/3 이 이 가정에서 나온다.**

---

## 3. 비용률 민감도 (3-5 작업 4)

`비용R = 왕복 비용률 / 스톱거리%` — 사이즈와 무관하다(Phase 1 실측).
gross 우위 **+1.950R** · 평균 스톱거리 **1.810%** 기준.

| 체결 전제 | 편도% | 왕복% | 비용R | netR | 우위/비용 | 금액 | PF | MDD |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **현행** (taker .06 + 슬립 .03) | 0.090 | 0.180 | 2.945 | **−0.995** | 1.51배 | −2.49 | 0.9109 | 13.95 |
| 테이커만 (.06 · 슬립 0) | 0.060 | 0.120 | 1.963 | **−0.013** | 1.01배 | −0.03 | 0.9988 | 12.99 |
| 메이커 + 슬립 .03 | 0.050 | 0.100 | 1.636 | **+0.314** | 0.84배 | +0.79 | 1.0296 | 12.67 |
| 메이커 + 슬립 0 (지정가) | 0.020 | 0.040 | 0.654 | **+1.296** | 0.34배 | +3.24 | 1.1269 | 11.73 |

**슬리피지 가정 하나가 부호를 결정한다.** 슬리피지를 0 으로 두는 것만으로
netR 이 −0.995 → −0.013 (본전선)이 된다.

> ⚠️ **지정가 행은 미체결을 모델링하지 않은 값이다.** 그대로 채택하면 낙관 편향이다(작업 5).
> 지정가는 "더 싸게 체결"이 아니라 "체결되거나 안 되거나"다. 안 되는 경우를 세지 않으면
> 좋은 자리만 골라 들어간 것처럼 보인다.

---

## 4. 결정 등록 (3-5 작업 3) — 결정 9

**운용 체결 방식이 확정되지 않았다. 임의 가정을 하지 않는다(C8).**

| 선택지 | 편도 비용 | 필요한 추가 모델 |
| --- | --- | --- |
| **A. 시장가 (현행 전제)** | 0.06% + 슬리피지 | 슬리피지 실측 근거 |
| **B. 지정가** | 0.02% | **미체결 모델 필수** — 없으면 낙관 편향 |
| **C. 혼합** (진입 지정가 · 청산 시장가) | 0.02% / 0.06% | 진입 미체결 모델 |

**B 를 채택하려면 미체결 모델이 반드시 동반된다**(작업 5). 확정봉 종가에 지정가를 걸면
가격이 그 자리를 떠난 경우 체결되지 않고, 그 거래는 표본에서 사라진다 —
그 손실을 세지 않으면 성적이 부풀려진다.

**확정 전까지 현행(A · 0.09%)을 유지한다.** 비용률을 낮춰 성적을 만드는 것은 조작이다(C8).

---

## 5. 슬리피지 근거 확보 방법 (미착수)

실계좌 체결 기록과 대조해야 한다:

```
실측 필요:  주문 시각의 호가 vs 체결가
현재 상태:  실계좌는 read-only 동기화만 하고 주문 실행이 없다(봉인)
```

**봉인 상태에서는 슬리피지를 실측할 수 없다.** 대안은 과거 호가 데이터로 추정하는 것인데,
`STOP_EXECUTION.md` 가 지적한 대로 **크립토 봉조차 보존하지 않는다.** 호가는 더더욱 없다.

> 슬리피지 근거 확보는 **관측 대상 저장**(로드맵 #4)에 종속된다. 그 전에는 0.03% 가
> 보수적 가정인지 낙관적 가정인지 알 수 없다. **추정치를 만들지 않았다.**

---

## 6. 이 문서가 바꾼 것 — 없다

```
taker_fee_pct       0.06   변경 없음 (거래소 실측과 일치)
slippage_pct        0.03   변경 없음 (근거 미확보 · C8)
체결가 산출          변경 없음
```

**계측과 결정 등록까지가 이 항목의 범위다**(WO 3-5 작업 6).

---

## 7. 금지

- 근거 없이 비용률을 낮추는 것 (C8)
- 지정가 전제를 채택하면서 미체결 모델을 생략하는 것
- 민감도표의 유리한 행을 대표값으로 보고하는 것 — 대표는 **현행 0.09% 행**이다
- 등급 할인을 가정에 넣는 것 — 확인되지 않았다

---

# Phase 4-1 — 슬리피지 관측 배선

> WO-FCE-RISK-SIZING-01 Phase 4-1. **배선 + 정의 등록. 비용률 변경 0줄**(C7).
>
> 실사 2026-08-19 · Bitget 공개 호가 · `slippage_pct` diff **0줄**

## 8. §5 의 "실측 불가"는 틀렸다

Phase 3-5 는 "봉인 상태에서는 슬리피지를 실측할 수 없다"로 미산출 처리했다. 그 전제가
너무 좁았다 — **실주문 없이도 결정 시점의 호가 깊이로 예상 체결가를 낼 수 있다.**

시장가 주문은 호가를 유리한 쪽부터 먹는다. 명목가를 호가창에 얹어 가중평균 체결가(VWAP)를
구하면 중간가와의 차이가 곧 예상 슬리피지다:

```
예상 슬리피지% = |VWAP(명목) − 중간가| / 중간가 × 100
```

이것도 추정이지만 0.03% 와 결정적으로 다르다 — **출처가 있고 재검증이 가능하다.**

정본 구현: [`app/paper/slippage.py`](../../backend/app/paper/slippage.py)

## 9. 저장 대상과 리텐션

호가는 **사후에 재구성할 수 없다.** 그래서 결정 시점에 남긴다. 크립토 봉 미보존
(`STOP_EXECUTION.md` §3)·와이코프 국면 미저장(`POSITION-VIEW-01`)과 같은 계열의 결함이며,
이번이 **세 번째**다.

| 항목 | 값 |
| --- | --- |
| 테이블 | `execution_depth_observations` (마이그레이션 `0036`) |
| 시점 | 진입 결정 시점 · 청산(부분·전량) 결정 시점 |
| 최소 저장 | 최우선 매수/매도 호가와 각 잔량 · 중간가 · 스프레드 |
| 산출 | 명목 **100 / 300 / 600** USDT 각각의 매수·매도 예상 슬리피지 |
| 깊이 고갈 | 명목을 못 채우면 `depth_exhausted=true` 로 **표시**한다(감추면 저유동성이 과소평가된다) |
| 조회 실패 | 사유를 담은 관측을 남긴다 — 건너뛰지 않는다(침묵 금지) |
| 리텐션 | `db_depth_observation_retention_days` 기본 **45일** · maintenance 경로에서 삭제 |

리텐션이 필요한 이유는 DB 가 이미 **10GB** 라는 것이다(건당 payload 가 호가 100단계×2면).
45일은 대체 기준(30건)을 채우고도 남는 창이다.

## 10. 가정 대체 기준 — **결과 확인 전에 고정했다**

`app/paper/slippage.py::ASSUMPTION_REPLACEMENT` 가 정본이며 테스트가 값을 고정한다
(`test_replacement_criteria_fixed_before_results`).

| 기준 | 값 | 왜 |
| --- | --- | --- |
| 최소 관측 수 | **30** | `REQUIRED_SAMPLE.md` 승격 기준과 같은 값. 새 기준을 발명하지 않는다 |
| 최소 심볼 수 | **3** | 한 심볼의 유동성이 전체를 대표하지 않는다 |
| 판정 통계 | **p80** | 평균은 꼬리에서 과소평가한다. 갭 1건이 초과분 56%를 만든 Phase 2 와 같은 교훈 |
| 교체 방향 | 관측 p80 < 0.03% 일 때만 **제안** | 낮추면 흑자가 나오므로 특히 보수적으로 |
| 자동 적용 | **금지** (`auto_apply=False`) | 도메인 불변(자동 승격 금지). 사용자 결정을 거친다 |

> **표본이 차기 전에는 0.03% 를 유지한다.** 판정은 `sample_insufficient` 를 돌려주며
> 방향을 말하지 않는다(C10).

## 11. 초기 관측 — N=5, **판정 아님**

배선 검증 중 얻은 값이다. **엔드포인트 형식을 확인하는 과정에서 관측했으므로 §10 의 기준을
"결과를 보기 전에" 정했다는 순서를 정확히 적는다: 기준은 이 표를 만들기 전에 코드로
고정됐고, 그 뒤 배선을 검증하며 아래 5건을 봤다.**

2026-08-19 · 명목 300 USDT 매수 기준 · 각 심볼 1회 스냅샷:

| 심볼 | 반스프레드% | 매수 100 | 매수 300 | 매수 600 | 가정 대비 |
| --- | --- | --- | --- | --- | --- |
| BTCUSDT | 0.00008 | 0.00008 | 0.00008 | 0.00008 | **375배 낮다** |
| ETHUSDT | 0.00026 | 0.00026 | 0.00026 | 0.00026 | 115배 낮다 |
| HYPEUSDT | 0.00255 | 0.00255 | 0.00257 | 0.00341 | 12배 낮다 |
| SPCXUSDT | 0.00690 | 0.00690 | 0.01077 | 0.01228 | 2.8배 낮다 |
| **BASEDUSDT** | 0.05632 | 0.06282 | **0.06842** | 0.07927 | **2.3배 높다** |

```
관측 5건: 평균 0.01642% · 중위 0.00257% · p80 0.02230% · 최대 0.06842%
가정      0.03%
판정      sample_insufficient  (30건·3심볼 요건 중 관측 수 미달)
```

### 이 표가 말하는 것 — 그리고 말하지 않는 것

**0.03% 는 일률적으로 보수적인 가정이 아니다.** 대형에서는 극단적으로 과대(BTC 375배),
최저유동성에서는 **과소**(BASED 2.3배)다. 즉 문제는 값의 높낮이가 아니라 **단일 상수라는
구조**다 — 심볼당 유동성이 3자릿수 차이인데 하나의 편도 비용률을 쓰고 있다.

말하지 않는 것:
- **N=5 는 판정 근거가 아니다.** 각 심볼 1회, 임의 시각 스냅샷이다
- 진입·청산이 몰리는 시각(봉 마감)의 호가가 아니다. 실제 결정 시점 표본은 아직 0건이다
- 변동성 국면별 차이를 전혀 담지 않았다. 갭이 난 순간의 호가는 이 표에 없다
- **비용률을 바꾸지 않았다** — `slippage_pct` diff 0줄

## 12. 이 항목이 바꾼 것

```
slippage_pct        0.03   변경 없음 (C7)
taker_fee_pct       0.06   변경 없음
execution_cost_rate        변경 없음
체결가 산출                 변경 없음
진입 게이트·사이징·출구      변경 없음

추가: execution_depth_observations 테이블 · 관측 배선 · 대체 기준
```

**관측 경로를 여는 것까지가 이 항목의 범위다.** 부호를 바꾸는 것은 표본이 찬 뒤의 일이다.

---

## 13. 첫 결정 시점 관측 (2026-08-19T12:12Z) — 가정이 낙관적일 수 있다

`DISCOVERY-UNBLOCK-01` 로 진입이 재개되면서 **실제 결정 시점**의 호가가 처음 저장됐다.
§11 의 N=5 는 배선 검증용 임의 시각 스냅샷이었고, 이것은 엔진이 실제로 진입을 결정한 순간이다.

| 심볼 | 결정 | 반스프레드 | 매수 300 | 매수 600 | 가정 0.03% 대비 |
| --- | --- | --- | --- | --- | --- |
| INTCUSDT | entry | 0.0157% | **0.0157%** | 0.0178% | 1.9배 낮다 |
| **DELLUSDT** | entry | 0.0605% | **0.0605%** | 0.0605% | **2.0배 높다** |

```
관측 2건: 평균 0.0381% · p80 0.0516% · 최대 0.0605%
가정      0.03%
판정      sample_insufficient  (30건·3심볼 요건 중 2건·2심볼)
```

### §11 의 N=5 는 오해를 유발했다 — 정정

§11 에서 BTC 0.00008%·ETH 0.00026% 를 근거로 "가정이 대형에서 극단적으로 과대"라고 적었다.
그 관측은 **엔진이 실제로 거래하지 않는 심볼**이었다. 실제 유니버스는 유동성이 훨씬 낮은
토큰화 주식·중형 알트이고, 첫 실측 2건의 **평균과 p80 이 모두 가정값을 넘는다.**

**즉 0.03% 는 보수적 가정이 아닐 수 있다.** §11 이 시사한 방향(비용 인하 → 흑자)과 반대다.

> ⚠️ N=2 다. 방향을 말하지 않는다 — `verdict` 가 `sample_insufficient` 인 것이 정본이다.
> 다만 §11 의 "375배 낮다" 같은 서술을 근거로 비용률 인하를 검토하는 것은 **금지**다.
> 표본은 **거래 대상 심볼에서** 30건을 채워야 한다(C7 · `ASSUMPTION_REPLACEMENT`).

### 표본 축적 경로가 열렸다

```
대체 기준   30건 · 3심볼
2026-08-19  2건 · 2심볼   (진입 재개 첫 봉)
```

진입·청산 결정마다 쌓인다. 속도 산정은 `validation/SAMPLE_RATE.md` 참조.
