# WO-FCE-86 — 실현 청산 밀집도 차트 가독성 개선

우선순위: P1 (현재 히트맵이 산발적 이벤트 셀만 보여 가격대 밀집과 현재가 관계를 즉시 해석하기 어렵다)
선행: WO-FCE-78, WO-FCE-85

## 착수 전 확인 (AGENTS.md 불변 규칙 2)
- [x] `git branch -a` 에 같은 WO 브랜치 없음
- [x] `git log --oneline -15` + `git status` 에 기존 구현 없음
- [x] `grep -r "WO-FCE-86" docs/ backend/ dashboard/` 결과 없음

## 진단 (코드 확정)
- `dashboard/components/position/LiquidationHeatmapPanel.tsx`는 실현 청산 셀만 SVG에 그리며 같은 분석 응답에 이미 있는 OHLC 캔들을 사용하지 않는다.
- 가격대 누적 강도는 하단 숫자 카드에만 있고 차트 내부 수평 밴드로 연결되지 않아, 현재가 위·아래 어느 구간에 실현 청산이 밀집됐는지 시각적으로 비교하기 어렵다.
- Bitget 데이터는 이미 발생한 청산 이력이다. Coinglass 예상 청산 레버리지 맵과 같은 미래 유동성 지도로 표현하면 안 된다.

## 작업
### 1. 가격 맥락 결합
- 공용 분석 화면의 확정 OHLC 캔들을 실현 청산 패널에 전달해 동일 시간·가격 축에 표시한다.

### 2. 수평 밀집 밴드
- 선택 기간의 가격대별 누적 실현 청산 강도를 수평 밴드와 순위 라벨로 표시한다.
- 기존 시간×가격 이벤트 셀은 실제 발생 시점 관측으로 유지한다.

### 3. 즉시 해석 요약
- 최대 밀집 가격대와 현재가 위·아래 누적 강도를 차트 상단에 표시한다.
- 범례와 카피에서 `기간 누적 실현 밀집`, `미래 예상 아님`을 명시한다.

## 수용 기준
- [x] 실현 청산 패널에 OHLC 캔들이 표시된다.
- [x] 상위 실현 밀집 가격대가 수평 밴드로 구분되고 최대 구간이 즉시 식별된다.
- [x] 현재가 위·아래 실현 강도와 최대 구간의 현재가 대비 거리가 표시된다.
- [x] 모바일에서 가로 오버플로 없이 읽을 수 있다.
- [x] 프론트 게이트·E2E·브라우저 시각 검증을 통과한다.

## 금지
- 실현 청산 이력을 미래 예상 청산 맵 또는 미체결 유동성으로 표현하지 않는다.
- 이 화면을 Entry Score, 방향 판정, 자동 진입, 후보 자동 승격에 연결하지 않는다.
- 미확정 캔들을 채점에 사용하거나 실주문 경로를 추가하지 않는다.

## 문서
- 갱신할 `docs/*.md`: `docs/Derivatives.md`, `docs/LiquidationIntelligence.md`

## 완료 정의 (공통)
- [x] HARNESS.md 게이트 통과
- [x] docs 갱신
- [x] origin/main 반영 + CI success 확인 (불변 규칙 1·3)
