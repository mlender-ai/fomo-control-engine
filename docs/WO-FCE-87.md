# WO-FCE-87 — 4주 페이퍼 검증 슬롯 지속 보충

우선순위: P0 (첫 유효 거래 뒤 부트스트랩이 종료되어 4주 표본 수집이 다시 정지)
선행: WO-FCE-77, WO-FCE-83

## 착수 전 확인 (AGENTS.md 불변 규칙 2)

- [x] `git branch -a` 에 같은 WO 브랜치 없음
- [x] `git log --oneline -15` + `git status` 에 기존 구현 없음
- [x] `rg "WO-FCE-87" docs backend dashboard` 결과 없음

## 진단

- 워커와 24시간 평가는 정상이나 최근 7일 207회 평가 중 확정 전환은 8회였다.
- 기존 `_bootstrap_validation_positions`는 유효한 벤치마크 거래가 단 한 건이라도 있으면 즉시 반환해, 그 거래가 끝난 뒤에도 다음 검증 표본을 만들지 않았다.
- 2026-07-18 현재 복구 로직이 ETHUSDT 4h 롱 페이퍼 포지션을 열었지만 이 일회성 시드만으로는 4주 N≥10 채점 표본을 안정적으로 확보할 수 없다.

## 작업

1. 벤치마크 기간 동안 최대 2개의 검증 슬롯을 유지한다.
2. 종료로 빈 슬롯이 생기면 다음 확정 캔들에서 성적·체크리스트·R:R·손절·신선도 게이트를 모두 통과한 후보만 보충한다.
3. 같은 심볼의 동일 확정 캔들 재진입을 금지한다.
4. 포지션 화면에 슬롯 가동 수와 보충 정책, 고정 크기·손절·실주문 없음 상태를 표시한다.

## 수용 기준

- [x] 첫 유효 거래가 종료된 뒤 새 확정 캔들에서 `validation_sampler` 거래가 생성된다.
- [x] 최대 동시 검증 슬롯은 2개다.
- [x] 기존 성적·체크리스트·R:R·무효화·청산 안전·신선도 게이트를 우회하지 않는다.
- [x] 화면에서 슬롯 가동 수와 다음 동작을 확인할 수 있다.
- [x] 미확정 캔들·실계좌 주문·자동 승격 경로가 생기지 않는다.

## 검증 기록

- 대상 테스트: `backend/tests/test_paper_policy.py`
- 화면 테스트: `dashboard/tests/e2e-smoke.spec.ts`
- 실제 상태: ETHUSDT 4h 롱 페이퍼 포지션 1개 가동, 검증 슬롯 `1/2`, 두 번째 슬롯은 현재 적격 후보가 생기는 다음 확정 캔들에서 보충 대기.
- 로컬 게이트: 백엔드 505 passed, 커버리지 77.12%, E2E 18 passed, 프로덕션 빌드·14개 라우트/29개 자산 2xx.

## 완료 정의 (공통)

- [x] HARNESS.md 게이트 통과
- [x] docs 갱신
- [x] origin/main 반영 + CI success 확인 (불변 규칙 1·3)
