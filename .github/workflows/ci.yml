name: CI

on:
  pull_request:
  push:
    branches:
      - main
  # 웹훅 누락 시 수동 재실행 경로.
  #
  # 2026-08-27: main 에 푸시(6cb971fd)했는데 워크플로 실행이 생성되지 않았다. 커밋은
  # 원격에 정상 도착했고 워크플로도 active 인데 트리거만 걸리지 않았다. 불변 규칙 3 은
  # green CI 를 요구하므로, 재실행 경로가 없으면 그 커밋을 영구히 검증할 수 없다 —
  # 빈 커밋으로 이력을 더럽히는 것이 유일한 수단이 된다.
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: backend
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: backend
    env:
      FCE_ENV: ci
      FCE_MARKET_DATA_PROVIDER: mock
      FCE_DATABASE_URL: "sqlite:///:memory:"
      FCE_RUN_LIVE_BITGET_TESTS: "false"
      FCE_WORKER_ENABLED: "false"
      FCE_TELEGRAM_BOT_ENABLED: "false"
      FCE_TELEGRAM_ALERTS_ENABLED: "false"
      FCE_OPENAI_API_KEY: ""
      FCE_COINGLASS_API_KEY: ""
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: backend/requirements-dev.txt

      - name: Install dependencies
        run: python -m pip install -r requirements-dev.txt

      - name: Ruff lint
        run: python -m ruff check .

      - name: Ruff format check
        run: python -m ruff format --check .

      - name: Import cycle check
        run: python scripts/check_import_cycles.py

      - name: Mypy baseline
        run: python scripts/check_mypy_baseline.py

      - name: Pytest with coverage
        run: python -m pytest --cov=app --cov-report=term --cov-report=xml --cov-report=json:coverage.json --cov-fail-under=70

      - name: Quality baseline
        run: python scripts/check_quality_baseline.py

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: backend-coverage
          path: |
            backend/coverage.xml
            backend/coverage.json

  frontend:
    name: frontend
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: dashboard
    env:
      NEXT_PUBLIC_API_BASE_URL: http://127.0.0.1:8875
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: dashboard/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: ESLint
        run: npm run lint

      - name: TypeScript
        run: npm run typecheck

      - name: Build
        run: npm run build

  e2e:
    name: e2e
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      NEXT_PUBLIC_API_BASE_URL: http://127.0.0.1:8875
      FCE_E2E_DATABASE_URL: sqlite:////tmp/fce-e2e-ci.db
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: backend/requirements-dev.txt

      - name: Install backend dependencies
        working-directory: backend
        run: python -m pip install -r requirements-dev.txt

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: dashboard/package-lock.json

      - name: Install frontend dependencies
        working-directory: dashboard
        run: npm ci

      - name: Install Playwright browser
        working-directory: dashboard
        run: npx playwright install --with-deps chromium

      - name: Playwright demo-mode smoke and visual tests
        working-directory: dashboard
        run: npm run test:e2e

      - name: Upload Playwright artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-artifacts
          path: |
            dashboard/test-results
            dashboard/playwright-report
