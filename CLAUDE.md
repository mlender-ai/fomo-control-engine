# Claude Code entrypoint

`AGENTS.md`를 먼저 읽어라. Claude Code·Codex·Cursor·Gemini 공용 헌법이다.

정본 우선순위: `docs/PRD.md` > `AGENTS.md` > `HARNESS.md` > 개별 `docs/*.md`.

규칙 본문을 여기 복붙하지 않는다 — 규칙이 바뀌면 `AGENTS.md`만 고친다.

push 전 게이트: `HARNESS.md`. 세션 종료 전: 불변 규칙 1(무조건 머지) 확인.
