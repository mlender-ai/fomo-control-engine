# FOMO Control Engine — 에이전트 헌법

> 이 저장소에서 작업하는 **모든 AI 에이전트**(Claude Code·Codex·Cursor·Gemini 등)는 작업 시작 전에 이 파일을 따른다.
> 정본 우선순위: `docs/PRD.md` > `AGENTS.md` > `HARNESS.md` > 개별 `docs/*.md`.
> 원칙: **Stop Prompting, Start Governing** — 규칙은 프롬프트가 아니라 게이트(CI·훅)로 강제한다.

---

## 🔴 불변 규칙 1 — 완료 작업은 무조건 머지한다

**세션에서 만든 작업물은 예외 없이 커밋하고 `origin/main`에 반영한다. 커밋·머지 안 된 채로 세션을 끝내지 않는다.**

과거 사건: WO 구현이 워킹트리 미커밋·codex 브랜치 미머지로 방치돼 "됐다"고 착각한 채 유실됨(WO-70/71/72). 재발 금지.

작업 종료 전 **반드시** 실행·확인:
1. `git status --short` — 비어야 한다(미추적 포함 전부 커밋).
2. `git branch -a` — 세션이 만든/받은 브랜치의 미머지 커밋을 main에 통합, 병합 끝난 브랜치 삭제.
3. `git rev-list --count origin/main..main` — **0**이어야 한다(푸시 완료).

예외는 단 둘: 파괴적/비가역 작업, 사용자의 명시적 보류 지시. 보류 시 `touch .git/FCE_STOP_OK`로 종료 훅을 1회 통과시킨다(다음 세션 시작 시 삭제).

## 🔴 불변 규칙 2 — WO 착수 전 중복 구현 검사

같은 WO를 두 에이전트가 각자 구현해 충돌한 사건(71이 2벌) 재발 금지. **WO 착수 전 반드시**:
1. `git branch -a` — 같은 WO 번호/주제의 브랜치가 있는지.
2. `git log --oneline -15` + `git status --short` — 이미 구현/진행 중인지.
3. `grep -r "WO-<번호>" docs/ backend/ dashboard/` — 기존 산출물 검색.

이미 있으면: 새로 만들지 말고 그 작업을 **완성·머지**한다. 두 구현이 이미 병존하면 정본 하나를 정하고 나머지는 유용한 부분만 흡수 후 제거(두 시스템 공존 방치 금지).

## 🔴 불변 규칙 3 — 게이트 통과 없이 push 금지

로컬에서 `HARNESS.md`의 게이트를 통과한 뒤에만 push 한다. push 후 **CI 결과를 확인**한다(`gh run list --branch main --limit 1` — failure 방치 금지). CI가 빨간 상태로 세션을 끝내지 않는다.

> 교훈(2026-07-14): ci.yml YAML 오타 하나로 CI가 5일간 0초 실패했고, 아무도 몰랐다. 그 사이 mypy 부채 44→174. **"CI가 있다"와 "CI가 살아있다"는 다르다.**

## 🔴 불변 규칙 4 — 실행 중인 Next 산출물 덮어쓰기 금지

**`next start`가 `.next`를 서비스하는 동안 같은 `.next`에 빌드하지 않는다.** 이전
서버가 메모리에 보관한 HTML과 새 CSS/JS 청크가 갈리면 전체 프론트가 무스타일로
깨진다. 2026-07-17 전 화면 장애의 직접 원인이며 재발 금지다.

프론트 프로덕션 갱신은 아래 순서만 허용한다:
1. 실행 중인 8876 프론트 서버를 종료한다.
2. `cd dashboard && npm run build`를 실행한다. `next build` 직접 호출로 `prebuild`를 우회하지 않는다.
3. `npm run start:local`로 새 빌드를 시작한다.
4. `npm run check:local-assets`가 전체 제품 라우트의 CSS/JS를 모두 2xx로 확인해야 완료다.

Playwright는 반드시 `npm run test:e2e`/`build:e2e`를 사용해 `.next-e2e`에 빌드한다.
`.next` 공유, 빌드 가드 비활성화, 자산 검사 실패 무시는 금지한다. 상세 원인·복구
절차는 [`docs/FrontendBuildSafety.md`](docs/FrontendBuildSafety.md)를 따른다.

---

## 도메인 불변 (트레이딩 엔진 안전 규정 — 코드보다 우선)

- **자동 승격 금지**: candidate→validated 승격은 제안-거부권(veto window) 경유만. 어떤 경로로도 자동 적용하지 않는다.
- **룩어헤드 금지**: 감지기·채점은 확정 캔들만 사용. 새 감지기는 룩어헤드 감사 통과 전 채점 금지.
- **정직한 표본**: 백테스트(사전확률)와 라이브(검증) 출처 분리, N·CI 명시, 표본 부족 시 판정 유보.
- **미검증=관측만**: 미검증 시그니처·고래는 화면 표시만, 방향 판정·자동 진입에 사용 금지.
- **실주문 없음**: 페이퍼는 read-only. 실계좌 주문을 실행하는 코드를 만들지 않는다.
- **적용된 마이그레이션 불변**: `backend/app/db/migrations/*.sql` 기존 파일 수정 금지 — 새 번호로 추가.
- **킬존 게이트**: 차트·UI·시각 고도화 WO는 발의 시 `docs/FCE-MOAT-STRATEGY-01.md` §2 킬존 대조 결과와 위생 필요성(장애·가독 불능·데이터 오표시)을 명시해야 한다. 명시 없으면 착수 금지.
- **신규 감지기 모라토리엄**: 기존 candidate 시그니처 중 validated 승격 1호가 나오기 전 신규 감지기(엘리엇·추가 하모닉 등) 추가 금지. 예외는 사용자 명시 승인.
  - *예외 발동 이력(2026-07-31, 사용자 명시 승인)*: WO-FCE-PNF-TARGET-01의 PNF 수평 카운트. 단 이는 **진입 시그널이 아니라 기존 와이코프 국면의 목표 계산기**이며 진입 판단 로직에 새 시그널을 추가하지 않는다. `engine="wyckoff"`를 재사용하고 candidate로 등록해 동일 승격 기준(N≥30·CI 하한·거부권)을 따른다. 정본: [`docs/PNFTarget.md`](docs/PNFTarget.md).
- **페이퍼 트랙은 진입과 청산이 쌍으로 정의되어야 한다**: 출구 없는 진입 경로는 미완성으로 간주한다. 진입 게이트만 있고 청산 판단·매도 주문 생성이 없으면 청산 표본이 0이 되어 승률·실현 R이 원리적으로 산출 불가해지고, 그 트랙의 모든 개선안이 측정 불능이 된다(2026-07 주식 페이퍼 사건: 매도 주문 생성 코드 부재로 진입 6건이 8일 방치). 새 페이퍼 트랙은 청산 규칙 없이 진입을 배선하지 않는다. 정본: [`docs/StockExitPolicy.md`](docs/StockExitPolicy.md).
- **승률 개선안은 한 번에 하나씩 검증한다**: 여러 개선안을 동시에 투입하면 무엇이 효과였는지 영원히 알 수 없다(교란). 각 후보는 단독으로 시그니처 등록·검증하며, 개선 주장은 CI 비겹침 기준으로만 한다. 표본이 0인 트랙에서는 설계를 바꾸지 않는다 — 그것은 개선이 아니라 추측이다. 후보 백로그: [`docs/WinRateImprovementBacklog.md`](docs/WinRateImprovementBacklog.md).
- **타임아웃 없는 대기는 장애다**: 워커 잡과 모든 외부 I/O(HTTP·거래소·DB)에 타임아웃이 필수다. 잡은 `asyncio.wait_for` 예산, 어댑터는 connect/read 타임아웃 — 두 겹으로 건다. 심장박동은 가장 단순한 경로여야 하며(네트워크·앱 서비스 호출 금지), 그 실패는 WARNING 이 아니라 ERROR 다. 감지 신호(하트비트)와 복구 조치 신호는 일치해야 한다 — 포트만 보는 감시는 매달림을 놓친다(2026-07-28 11.7시간 사고). 정본: [`docs/EngineLiveness.md`](docs/EngineLiveness.md).
- **텔레그램 발송 관문은 하나다**: 발송 허용 목록은 **전역**이며 경로별로 두지 않는다. 새 알림은 `app/notify/delivery_gate.py` 레지스트리에 명시 등록해야 푸시되고, 미등록 `rule_id`는 **기본 차단**된다 — 등록 행위가 곧 검토 지점이다. 관문이 둘이면 그중 하나는 반드시 잊힌다(2026-08-08 고래 스팸: 페이퍼 이벤트 화이트리스트가 `AlertCandidate` 경로에 아예 적용되지 않아 미검증 신호가 우회 발송됐다). 미검증 신호는 강등하며, 강등은 **발송 경로만**이다 — 수집·저장·조회는 유지하고 미발송 사유를 남긴다(침묵 금지). 승격은 자동으로 일어나지 않는다. 정본: [`docs/PaperObservability.md`](docs/PaperObservability.md).
- **생존 감시는 프로세스 밖에 하나 이상**: 감시 경로를 추가·변경할 때 최소 1개는 **워커 프로세스 외부**여야 한다. 내부 감시만 추가하는 변경은 반려 — 워커가 죽으면 감시도 함께 죽어 침묵이 스스로를 은폐한다(2026-07-23 3트랙 4일 정지 사고). 뮤트는 조건 알림만 억제하며 생존/사망 신호를 끄지 못한다. stale 판정은 `last_success_at`이 아니라 `last_effective_run_at` 기준이다. 정본: [`docs/EngineLiveness.md`](docs/EngineLiveness.md).

## 코드 규율 — 게으름 사다리

새 코드 작성 전 순서대로 자문, 멈추는 첫 칸에서 멈춤: ① 존재할 필요 있나(YAGNI) ② 코드베이스에 이미 있나(재사용) ③ 표준 라이브러리 ④ 깔린 의존성 ⑤ 한 줄이면 한 줄 ⑥ 그제서야 최소 구현. 단 **게으르되 부주의 금지** — 도메인 불변·검증·에러 처리는 잘라내지 않는다.

## 멀티 에이전트 핸드오프 (Claude ↔ Codex)

작업을 넘기거나 세션을 마칠 때 마지막 출력(커밋 본문·이슈 코멘트 등)에 HANDOFF 블록을 남긴다:

```
HANDOFF
- 목표: <한 줄>
- 한 일: <건드린 파일·핵심 변경>
- 안 한 일/막힌 곳: <남은 작업·블로커>
- 다음 액션: <다음 에이전트가 바로 할 일 1~3개>
- 검증: <통과/실패한 게이트>
- 머지: <origin/main 반영 여부 — 불변 규칙 1>
```

## 작업 산출물 규약

- 새 WO는 `docs/templates/WO_TEMPLATE.md` 형식으로 고정 후 구현.
- WO 구현은 관련 `docs/*.md` 갱신을 동반한다(코드만 있고 문서 없는 기능 금지).
- 이 저장소는 **main 직접 커밋·푸시** 방식(PR 게이트 없음). 커밋 메시지에 WO 번호 명시.

## 시크릿

`.env`·`*.pem/key` 등은 편집 금지(Claude는 `.claude/hooks/protect-secrets.sh`가 결정론 차단). 변경이 필요하면 `.env.example`에 반영하고 실제 값은 사용자가 직접 관리.
